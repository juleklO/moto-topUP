{
  "name": "moto-topup",
  "version": "1.0.0",
  "description": "A “light architecture” full-stack web application for tracking vintage motorcycle fuel economy as a diagnostic signal (MPG as engine health telemetry). The stack is intentionally conservative and durable: **PostgreSQL + Apache/PHP (thin REST API) behind Nginx (reverse proxy)**, fully **Dockerized** for repeatable dev and deployment.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx @tailwindcss/cli -i ./src/assets/css/input.css -o ./src/public/css/style.css --watch",
    "build": "npx @tailwindcss/cli -i ./src/assets/css/input.css -o ./src/public/css/style.css --minify"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/juleklO/moto-topUP.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/juleklO/moto-topUP/issues"
  },
  "homepage": "https://github.com/juleklO/moto-topUP#readme",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "tailwindcss": "^4.1.18"
  }
}
